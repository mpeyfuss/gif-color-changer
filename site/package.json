{
  "name": "gif-color-changer-site",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare-assets": "bun run scripts/prepare-assets.ts",
    "dev": "bun run prepare-assets && vite",
    "build": "bun run prepare-assets && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "pyodide": "314.0.7",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
